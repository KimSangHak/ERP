<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuhannci.erp.mapper.SystemConfigMapper">

	<select id="selectValue" parameterType="HashMap" resultType="String">
	
		select val from system_config where code = #{key}
	
	</select>


</mapper>